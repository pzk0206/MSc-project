# 项目当前状态

> AI 任务入口：开始项目工作前先阅读本文件。

最后更新：2026-08-01

## 当前阶段

三条核心 Cornell 实验流程、确定性单头/多头五随机种子对照以及 image-wise
五折均已完成。两个架构的 885 条合并预测和成对比较已经独立审计。中文论文
初稿、数字、引用和 PDF 已验证。导师建议的 PyBullet 第一阶段感知 pilot
及固定三物体目标选择研究均已完成真实 GPU 验证。三条明确 prompt 通过当前
二维感知门控；geometry、正式 single seed 42 和正式 multi-head seed 42
也已对同一定位框完成仿真接口检查。三个目标乘三个后端的二维中心现已用米制
深度和相机矩阵恢复为世界坐标，并通过重投影、segmentation 和真实射线九点
门控。九点结果现已进一步生成 18 个确定性俯视悬停候选，并完成 Panda 离线
IK/FK 与 41 状态碰撞余量审计。分阶段物理执行的阶段 1 已通过真实
`POSITION_CONTROL` 与 `stepSimulation` 完成安全空中往返，阶段 2 已使用
cube 真值位姿完成目标正上方 pregrasp，阶段 3 也已让张开的夹爪垂直下降到
接触前高度，阶段 4 已进一步下探到可闭合深度并取得持续双指目标接触。阶段 5
现已冻结阶段 4 的夹爪命令，将 cube 垂直抬升并稳定保持 240 步，完成一次
真值姿态仿真抓取。后续范围保持为几何与多头 CNN 两个最终候选：冻结阶段
1--5 控制链后，以相同场景和控制参数依次接入两个感知后端。当前结果验证的
是真值位姿控制链，不是感知后端抓取成功率。

## 已完成的实验流程

1. **传统计算机视觉基线**
   - 在整幅 RGB 图像上进行颜色/亮度阈值分割。
   - 提取轮廓并计算最小面积旋转外接矩形。
   - 使用 Cornell 抓取矩形指标评估预测抓取框。
2. **VLM 引导的几何实验流程**
   - 使用 Grounding DINO 和文本提示词完成目标定位。
   - 在定位框内运行 OpenCV 几何抓取后端。
3. **VLM 引导的 CNN 实验流程**
   - 使用 Grounding DINO 产生目标裁剪区域。
   - 使用轻量 CNN 回归抓取矩形参数。
   - 支持单头、多头、单次实验和五次重复实验汇总。
4. **PyBullet 虚拟相机感知 pilot**
   - DIRECT 模式加载桌面、Franka Panda 和 `duck_vhacd.urdf`。
   - 固定虚拟相机生成 RGB、米制深度和 segmentation。
   - RGB 接入现有 Grounding DINO、几何和 CNN 抓取后端接口。
   - 保存定位、二维抓取预测和完整元数据；明确
     `physical_grasp_executed: false`。
   - 感知阶段之后另设深度反投影门控；该第一阶段本身不调用 IK 或夹爪执行。
5. **PyBullet 静态姿态与运动学审计**
   - 将通过九点门控的二维抓取转换为两个世界 `-Z` 俯视悬停候选。
   - 按 URDF 名称解析 Panda，审计 IK、关节限位、FK 误差与离散碰撞余量。
   - 只在 DIRECT 中静态设置关节；不执行电机、连续轨迹、闭合或物理抓取。
6. **PyBullet 阶段 1 安全空中运动冒烟**
   - 使用中立姿态 FK 构造世界 Z 方向高 `0.05 m` 的安全空中点。
   - 静态 IK/FK 与 `2 mm` 余量通过后，电机执行中立位→安全点→中立位。
   - 手指保持 `0.04 m` 张开；不靠近目标、不闭合、不接触、不抬升。
7. **PyBullet 阶段 2 真值方块上方到达**
   - 读取固定 cube 的真值中心、姿态和 AABB 顶面，不使用感知后端。
   - 目标稳定性、IK/FK 和静态余量通过后，执行中立位→俯视 pregrasp。
   - 逐步记录方块真值与末端相对位置；手指保持张开，不下降到接触高度。
8. **PyBullet 阶段 3 张开夹爪垂直接近**
   - 从已通过的 pregrasp 沿世界 Z 方向下降 `0.10 m`。
   - 到达 cube 顶面以上 `0.02 m` 的接触前姿态，双指继续保持张开。
   - 保存两段轨迹和空接触事件表；不闭合、不评价接触、不抬升。
9. **PyBullet 阶段 4 真值方块双指接触**
   - 重放阶段 2/3 门控后，让张开的夹爪从顶面以上 `0.02 m` 短距离下探到
     `0.005 m` 可闭合抓取深度。
   - 缓慢闭合双指；首次左右手指同一步产生目标正法向力后冻结命令并保持。
   - 保存五段轨迹、真实接触事件和五张关键帧；不执行抬升，不计抓取成功。
10. **PyBullet 阶段 5 真值方块抬升与保持**
    - 完整重放阶段 2--4，冻结阶段 4 的最终双指命令，不额外收紧或重新抓取。
    - 工具沿世界 Z 上升 `0.12 m`，方块离桌后完整保持 240 个仿真步。
    - 保存七段轨迹、接触事件和七张关键帧；本阶段通过后只计一次真值姿态
      仿真抓取成功，不代表几何或 CNN 感知后端成功。

## 最新已验证结果

### 完整 Cornell 数据集（885 个样本）

| 方法 | 成功数 | 成功率 | 平均最佳 IoU | 平均角度误差 |
|---|---:|---:|---:|---:|
| 传统计算机视觉基线 | 504 / 885 | 56.95% | 0.3360 | 29.62° |
| VLM + 几何后端 | 649 / 885 | 73.33% | 0.4182 | **14.81°** |
| 单头 CNN（确定性五 seed 均值 ± 标准差） | — | 74.55% ± 1.77% | 0.4535 ± 0.0149 | 16.62° ± 0.83° |
| 多头 CNN（确定性五 seed 均值 ± 标准差） | — | **75.59% ± 1.90%** | **0.4640 ± 0.0165** | **15.20° ± 0.29°** |

原始 73.11% 单次实验和旧五轮目录继续作为历史证据保留，但不进入最终公平
对照。正式确定性单头和多头实验均使用 seeds 42–46，并分别隔离保存每个
seed 的权重、历史、逐样本预测、summary 和真实最佳验证损失。

### 固定测试子集（Cornell 目录 09–10，共 85 个样本）

| 方法 | 测试集成功率 |
|---|---:|
| VLM + 几何后端 | 75.3%（64 / 85） |
| 单头 CNN（确定性五 seed） | **80.47% ± 5.19%** |
| 多头 CNN（确定性五 seed） | **80.47% ± 3.38%** |

Grounding DINO 使用提示词 `small object`，在当前实验中成功定位全部 885 个 Cornell 样本。

### Image-wise 五折正式结果

正式 manifest 使用 seed 42，覆盖 885 个唯一 sample ID。每折为 566 个训练、
142 个验证和 177 个测试样本；五个测试集合两两互斥。JSON SHA-256 为
`b7d3e22a145f50add6d57a70bf0abb87b4b12ee674541deab0d7fee9a286bc2d`，
单头和多头必须共同使用该文件。

| 架构 | 成功数 | Pooled 成功率 | 平均 IoU | 平均角度误差 |
|---|---:|---:|---:|---:|
| 单头 | 635 / 885 | 71.75% | 0.4390 | 17.74° |
| 多头 | 647 / 885 | **73.11%** | **0.4580** | **17.40°** |

单头五折成功率均值的总体标准差为 2.02 个百分点，多头为 2.98 个百分点。
多头相对单头提高 12 个成功样本（+1.36 个百分点）和 0.0190 IoU，并将平均
角度误差降低 0.34°。两个架构各有 885 个唯一测试 ID、每折 177 行，所有
数值有限，且每折最佳验证损失与保存历史的最小值一致。

Object-wise 需要图像到物体实例的权威映射；当前数据和已检查来源没有提供
可审计映射，`01`–`10` 也不是 object ID，因此不生成 object-wise 结果。

### PyBullet 第一阶段验证

- PyBullet `3.2.7`、API `202010061` 已安装到 `msc-grasp` 环境。
- 沙箱内无法访问 GPU，`torch.cuda.is_available()` 为 `False`；同一 Conda
  环境在沙箱外验证为 `True`，设备为 `NVIDIA GeForce GTX 1650 Ti`。
- 当前共有 115 项 simulation 测试；完整项目回归为 `147 passed`。
- DIRECT + TinyRenderer + CUDA 的真实 geometry pilot 成功生成 640×480
  RGB、深度、segmentation、定位、预测和 metadata。
- 默认 generic prompt `small object` 返回了可见框，但人工检查确认它框住
  Panda 末端而非黄色小鸭：box `[323, 68, 450, 150]`，score `0.5985`。
- 只改变 prompt 为 `yellow rubber duck` 后，Grounding DINO 正确框住小鸭：
  box `[318, 208, 374, 273]`，score `0.6985`；几何后端输出有限合法的二维
  抓取框。该单案例仅支持系统可行性和 prompt 歧义分析，不支持成功率结论。
- 固定多物体场景同时包含黄色鸭、红色方块、绿色球体和 Panda distractor；
  一次渲染、一次模型加载后依次运行三条明确 prompt 和 `small object`。
- 三条主 prompt 均唯一匹配指定目标：鸭 IoU `0.8597`、方块 `0.8717`、
  球体 `0.8485`，目标选择率为 `3/3`，平均指定目标 IoU `0.8600`。
- 三个几何抓取中心均位于各自 segmentation mask 内；人工检查同时发现抓取
  框宽度普遍超出物体轮廓，因此不能据此声称夹爪闭合或物理抓取成功。
- generic prompt `small object` 选择红色方块，box
  `[297, 189, 344, 245]`、score `0.5933`，不计入三目标成功率。
- 首次真实运行暴露 Panda 基座与桌体初始穿透，球体被动力学求解器弹出画面；
  将固定研究场景的 Panda 基座放到桌面高度后，初始接触为零且四个评价实体
  保持可见。该问题已有专门回归测试。
- 真实产物位于
  `data/processed/pybullet/multi_object_study/`；metadata 保持
  `segmentation_used_as_model_input: false` 和
  `physical_grasp_executed: false`。
- geometry、single seed 42 和 multi-head seed 42 在三条正确目标上复用
  同一 RGB 与同一定位框，共生成九条后端诊断。九组参数均有限、宽高为正、
  抓取中心位于各自目标 mask 内，旋转框均保持在 640×480 图像范围内。
- 鸭子上，geometry 为 `0°` 且框较宽，single 为 `-76.23°`，multi-head
  为 `-84.19°`；两个 CNN 框在图像中明显更紧。
- 方块上，geometry、single、multi-head 角度分别为 `0°`、`-42.67°` 和
  `-6.18°`；球体上分别为 `-90°`、`-4.73°` 和 `3.87°`。球体近似对称，
  不能根据方向差异判定哪个后端更好。
- 该单场景没有仿真抓取真值，`backend_comparison.json` 明确保持
  `performance_ranking_computed: false`；九个框通过几何检查不等于物理
  抓取成功。
- 九个保存的二维中心均按最近像素（半向上取整）读取米制深度，再通过保存的
  PyBullet view/projection 矩阵恢复相机和世界坐标；segmentation 与
  `rayTest` 仅在坐标生成后进行真值审计。
- 真实 CUDA 重跑得到完整 `3 × 3` 九条结果：有限坐标、有效深度、重投影、
  segmentation 目标匹配和射线目标匹配均为 `9/9`，总门控通过。
- 九点最大重投影像素误差为 `8.04e-13 px`，最大深度往返误差为
  `3.57e-7 m`，分别低于 `1 px` 与 `1e-4 m` 门槛；最大反投影点到射线命中点
  距离为 `0.00676 m`，只作表面一致性诊断，不是额外通过门槛。
- `backprojection_results.csv` 和 `backprojection_summary.json` 位于真实多物体
  产物目录；元数据明确记录深度只在二维预测后使用、segmentation/ray 不作
  坐标输入、`ik_executed: false` 和 `physical_grasp_executed: false`。
- 九条输入各生成 `0°/180°` 两个世界 `-Z` 俯视悬停候选；18/18 个 pregrasp
  与 surface-standoff 候选通过七关节 IK、关节限位及 `5 mm/5°` FK 门槛。
- 每个候选对中立→pregrasp→standoff 两段各采样 21 个状态（41 个唯一状态），
  手指保持 `0.04 m` 张开，碰撞余量固定为 `2 mm`。
- 12/18 候选通过碰撞余量，6/9 条输入选出确定性低代价候选；cube 和 sphere
  各三条均有候选，duck 的三条两个对称候选均因环境余量失败。因此
  `scientific_gate_passed: false`，没有放宽阈值。
- 审计只豁免从 URDF 推导的固定关节刚体内部接触和 Panda 底座—桌面安装接触；
  目标物体不豁免。输出位于 `pose_ik_candidates.csv`、`pose_ik_summary.json`
  和 `pose_ik_metadata.json`，后者记录输入哈希及全部非执行标志。
- 阶段 1 使用真实 Panda 位置电机执行安全空中往返，共记录 `506` 个仿真步；
  安全点和返回中立位的位置误差分别为 `0.0005104 m` 与 `0.0004816 m`，方向
  误差均为 `0°`，低于 `5 mm/5°` 门槛。
- 动态轨迹的最小记录余量为 `0.002 m`，环境与自碰撞计数均为零，所有状态
  有限；双指相对 `0.04 m` 张开目标的最大误差为 `1.67e-6 m`，阶段科学门控通过。输出位于
  `data/processed/pybullet/grasp_execution/stage_1_safe_motion/`，包含 506 行
  `state_trace.csv`、summary、metadata 和 640×480 起点/安全点/返回图像。
- 阶段 1 metadata 中电机、仿真步进和轨迹执行为 `true`；perception、目标
  接近、夹爪闭合、接触评价、物体抬升和物理抓取均保持 `false`。
- 阶段 2 使用固定 cube 真值顶面生成高 `0.12 m` 的俯视 pregrasp，并通过
  Panda 位置电机记录 `267` 个真实仿真步；末端位置误差为 `0.0009529 m`、
  方向误差为 `0.0341°`，相对方块中心 XY 偏差为 `0.0006632 m`，均低于
  `5 mm/5°` 与 XY `5 mm` 门槛。
- 阶段 2 的方块最大位移为 `1.96e-5 m`，低于 `0.001 m` 稳定门槛；双指
  张开最大误差为 `1.75e-5 m`，动态最小记录余量为 `0.002 m`，环境与
  自碰撞计数均为零，所有状态有限，科学门控通过。
- 阶段 2 输出位于
  `data/processed/pybullet/grasp_execution/stage_2_cube_pregrasp/`，包含 267 行
  逐步方块/末端相对轨迹、summary、metadata 和 640×480 起点/pregrasp 图像；
  图像已人工检查。metadata 明确保持 perception、下降到接触、闭合、接触
  评价、抬升和物理抓取为 `false`。
- 阶段 3 先执行 267 步 pregrasp，再执行 255 步垂直接近，共记录 `522` 个
  真实仿真步；approach 终点位置误差 `0.0007770 m`、方向误差 `0.00736°`、
  相对 cube 中心 XY 偏差 `0.0001064 m`，均通过既定门槛。
- approach 工具点实际位于本次 cube 顶面以上 `0.020769 m`，相对计划
  `0.02 m` 的高度误差为 `0.0007688 m`；cube 最大位移 `3.40e-5 m`，双指
  张开最大误差 `1.75e-5 m`，闭合命令数为零。
- 阶段 3 动态最小记录余量为 `0.002 m`，环境与自碰撞均为零，所有状态有限，
  科学门控通过。输出位于
  `data/processed/pybullet/grasp_execution/stage_3_open_approach/`，包含 522 行
  状态轨迹、只有表头的接触事件 CSV、summary、metadata 和三张 640×480
  关键帧；图像已人工检查。metadata 中接触下降、闭合、接触评价、抬升和
  物理抓取仍为 `false`。
- 阶段 4 的首次真实诊断确认 `0.02 m` 是安全接触前高度：仅闭合时手指碰撞
  几何仍高于 cube，每指最终到目标约有 `0.01173 m` 间隙。保持既有接触判据
  不变，将阶段 4 增加 `0.015 m` 开放夹爪短下探至顶面以上 `0.005 m` 后，
  双指接触自然成立且无禁止碰撞。
- 正式阶段 4 依次执行 `267/255/240/93/120` 步 pregrasp、approach、
  grasp-depth、close 和 contact-hold，共记录 `975` 步；抓取深度实际位于
  cube 顶面以上 `0.005589 m`，高度误差 `0.0005893 m`。
- 双指在闭合第 `93` 步首次同时接触，运行结束连续保持 `121` 步；左右最大
  法向力分别为 `20.49 N` 和 `19.15 N`。cube 最大位移 `0.0001181 m`，环境
  碰撞、自碰撞和非手指 cube 接触均为零，全部状态有限，总科学门控通过。
- 阶段 4 产物位于
  `data/processed/pybullet/grasp_execution/stage_4_bilateral_contact/`，包含
  975 行状态轨迹、468 条接触记录、summary、metadata 和五张 640×480
  关键帧；图像已人工确认双指闭合在方块两侧且方块仍在桌面。metadata 中
  `object_lifted` 和 `physical_grasp_executed` 仍为 `false`。
- 正式阶段 5 完整执行 `267/255/240/93/120/242/240` 步 pregrasp、approach、
  grasp-depth、close、contact-hold、lift 和 lift-hold，共记录 `1457` 个状态；
  其中抬升插值 240 步后又用 2 个 settle 步达到既定 `0.01 rad` 关节终点门槛。
- 最后 240 个保持状态中，cube 相对闭合参考位置的最小上升量为
  `0.116738 m`，最终上升量为 `0.120003 m`，均高于 `0.10 m` 门槛；cube 与
  桌面接触计数为零，最大末端—cube 相对漂移为 `0.001410 m`，低于 `0.01 m`。
- 抬升末端位置/方向误差为 `5.38e-5 m/0°`，七关节最大终点误差为
  `0.009347 rad`；抬升与保持期间左右最大法向力为 `16.05 N/16.09 N`，末段
  双指接触连续 482 步。环境、自碰撞和非手指 cube 接触均为零，所有状态有限，
  `physical_grasp_success` 与总科学门控通过。
- 阶段 5 产物位于
  `data/processed/pybullet/grasp_execution/stage_5_truth_cube_lift/`，包含 1457 行
  状态、2396 条接触记录、summary、metadata 和七张 640×480 关键帧；closed、
  lifted 和 lift-hold 已人工确认方块被双指夹持、离开桌面并保持。本结果只支持
  “一次真值姿态仿真抓取成功”，尚不支持任何感知后端成功率结论。

## 已确认的主要发现

- VLM 定位前端带来的提升最大：成功率从 56.95% 提升至 73.33%。
- CNN 后端在固定的 85 个测试样本上高于几何后端；由于目录 09–10 的物体
  构成和难度无法确认与其他分组可比，该结果不解释为一般性的未见物体泛化。
- CNN 后端的平均 IoU 更高，位置和尺寸预测更准确。
- 几何后端的角度误差更低，说明“物体长轴的垂直方向”仍是有效的抓取角度先验。
- 当前主要瓶颈已从目标定位转移到抓取框后端。
- 数据划分审计发现传统 CV 在测试子集上的成功率为 74.12%，明显高于训练
  子集的 53.17%；这进一步要求谨慎解释测试结果。
- 固定 85 样本的逐样本交叉比较为：两者都成功 52、仅 CNN 成功 16、仅几何
  成功 12、两者都失败 5，说明两个后端具有可量化的互补性。

## 当前仓库状态

- 核心实现位于 `src/shared/`、`src/baseline_cv/` 和 `src/vlm/`。
- 实验数据和产物位于被 Git 忽略的 `data/` 目录。
- 毕业论文 LaTeX 框架位于 `uog_dissertation_outline/`。
- `uog_dissertation_outline/l4proj.tex` 已覆盖为完整中文初稿；英文版本可从
  Git 提交 `a4d62d6` 恢复。
- 中文 PDF 已用 Tectonic 编译为 29 页，并回读摘要、目录、七个主要章节、
  表/图标签和参考文献；12 个 citation key 与 BibTeX 条目全部匹配。
- 论文已在不新增第四个研究问题的前提下补入 PyBullet 坐标验证方法、九点
  结果表、分层讨论、结论边界及复现附录；表格页与方法页视觉检查无越界。
- 旧模板的 `agsm.bst` 在当前 BibTeX 引擎中产生 entry-mutation errors，
  因此初稿改用 natbib 兼容的 `plainnat` 作者—年份参考文献样式；BibTeX
  条目内容和引用 key 未改变。
- 项目文档已按 AI 上下文、调试、计划和工作日志分类，说明文字统一使用中文。
- 文档整理没有改变任何实验代码、模型行为或结果文件。
- PyBullet pilot 位于 `src/simulation/pybullet/`；生成结果位于被 Git 忽略的
  `data/processed/pybullet/`，不作为提交源码。

## 下一步重点

论文已包含中文摘要、引言、文献综述、方法论与研究伦理、按三个研究问题组织
的研究结果、综合讨论、结论和复现附录。文献综述重点批判分析矩形候选评分、
直接回归、密集生成、开放词汇定位和语言驱动抓取；当前轻量网络明确定位为
432,454 参数的受控回归基线，不声称为新颖架构或最新水平。

固定多物体 prompt、三后端、深度反投影、俯视姿态、离线 IK/FK 与离散碰撞
审计均已实现。物理执行扩展已决定只比较几何与多头 CNN；单头继续作为二维
架构基线，不进入后续物理比较。阶段 1--5 的真值 cube 控制链已经全部通过，
包括双指闭合、垂直抬升和 240 步保持。下一阶段先冻结该控制链，设计几何与
多头后端在同一 cube 场景、同一定位框和同一控制参数下的公平感知 pilot；
完成单次配对 pilot 后再决定 sphere、duck 和批量成功率协议。
论文仍优先逐章检查论证口吻、导师要求、最终提交语言及参考文献格式；不重新
运行数字未受影响的 Cornell 正式实验。

### 已确认的正式实验决策

1. 修复后的单头和多头 seeds 42–46 正式对照已经完成并审计。
2. 使用共同 manifest 运行 image-wise 五折；训练 seed 固定为 42，每个 fold
   分别保存模型、历史、177 条测试预测和 summary。
3. Object-wise 只有在获得权威实例映射后才实现；当前保持元数据阻塞状态，
   不使用目录或视觉相似度推断 object ID。
4. 五个随机种子与五折交叉验证分开报告；前者描述训练随机性，后者描述
   image-wise 划分变化。
5. 保留当前 `01–06/07–08/09–10` 固定目录结果，用于后端逐样本比较和失败
   分析，但不再称为标准 Cornell object-wise protocol。
6. RGB-D 不加入本月正式主实验。当前研究继续限定为 Grounding DINO RGB
   crop 下的轻量抓取后端比较；深度融合写入局限与未来工作，避免同时扩大
   输入模态、网络结构和评价协议。
7. 正式单头/多头实验启用严格确定性设置。仅调用随机种子但未关闭 CUDA
   非确定性的批次保留为诊断记录，不进入最终公平对照。
8. PyBullet 第一阶段只作为从 Cornell 静态图像到机器人式虚拟相机输入的
   系统集成与失败模式 pilot；不得称为真实抓取或仿真抓取成功率实验。
9. 多物体研究的 segmentation 只作事后真值评价。`0.25` 是固定 pilot 的
   目标框 IoU 工程门槛，不是 Cornell 抓取矩形指标；generic prompt 只作
   诊断，不进入三目标成功率。
10. 仿真三后端检查固定使用正式 single/multi-head seed 42 权重。没有仿真
    抓取真值时，只报告有限性、中心 mask 和图像边界，不对后端进行性能排名。
11. 后续物理执行只保留 VLM + 几何和 VLM + 多头 CNN 两个互补候选；传统
    CV 与单头 CNN 保留在 Cornell 二维对比中。
12. 物理执行先用 cube 真值坐标逐级验证运动、接近、闭合和抬升，之后才用
    感知预测替代真值输入；只有通过抬升和保持门控才计为仿真抓取成功。
13. 阶段 1 已通过真实电机安全空中往返门控；阶段 2 只能增加真值 cube 上方
    pregrasp 到达，不得同时实现下降、闭合或抬升。
14. 阶段 2 已通过真值 cube 正上方到达门控；阶段 3 只能增加张开夹爪的垂直
    接近，不得同时闭合、评价接触或抬升。
15. 阶段 3 已通过接触前垂直接近门控；阶段 4 只能增加双指闭合与目标接触
    评价，不得同时抬升物体。
16. 阶段 4 已通过开放夹爪短下探、双指闭合和持续目标接触门控；阶段 5 只能
    增加真值 cube 的垂直抬升与保持，只有抬升和保持通过才计仿真抓取成功。
17. 阶段 5 已完成一次真值姿态仿真抓取：cube 上升至少 `0.10 m`、离开桌面
    并保持 240 步。后续不得调整已冻结的闭合/抬升控制来适配某个感知后端，
    且该单次真值结果不得写成感知后端成功率。

### 后续工作顺序

1. 用户按摘要、引言、文献综述、方法论、结果、讨论和结论顺序审阅中文初稿。
2. 根据导师反馈补充或压缩论证，并确认学院字数、参考文献格式和提交语言。
3. 冻结已通过的阶段 1--5 真值 cube 控制链及其阈值、场景和失败分类。
4. 设计并实现 VLM + 几何与 VLM + 多头 CNN 的公平 cube 配对 pilot；两个
   后端必须复用同一图像、定位结果、场景初态和控制参数。
5. 两个 cube pilot 均完成逐阶段证据后，再冻结正式配对成功率协议；duck 的
   现有余量失败单独诊断，不通过放宽真值控制门槛掩盖。
6. 权威 object-wise、RGB-D 和真实机器人验证继续作为后续研究，不扩张为
   当前已完成结论。

## 后续阅读

- 项目研究背景与方法：[项目概览](PROJECT_OVERVIEW.md)
- 当前代码和数据结构：[项目结构](PROJECT_STRUCTURE.md)
- 新代码放置与拆分规则：[代码组织规范](CODE_ORGANIZATION.md)
- 完整工作时间线：[项目工作日志](../worklog/WORKLOG.md)
- 调试历史：[调试记录](../debugging/BUGLOG.md)
- 失败案例分析：[失败案例分析](../debugging/FAILURE_ANALYSIS.md)
- 研究计划：[VLM 机器人抓取研究计划](vlm_robotic_grasp_study_plan.md)

## 维护规则

仅在项目阶段、已验证指标、主要结论或下一步优先级发生变化时更新本文件。历史过程写入 `docs/worklog/`，详细调试记录写入 `docs/debugging/`；不要在这里记录未经验证的实验结果。
